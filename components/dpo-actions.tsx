"use client";
import { useRouter } from "next/navigation";
export function SdfToggle({enabled}:{enabled:boolean}){const router=useRouter();return <button className="btn secondary" onClick={async()=>{await fetch("/api/settings",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({sdfMode:!enabled})});router.refresh();}}>{enabled?"Disable":"Enable"} SDF mode</button>}
export function RemediationReview({id}:{id:string}){const router=useRouter();async function act(action:"approve"|"reject"){const reason=action==="reject"?window.prompt("Rejection reason")?.trim():undefined;if(action==="reject"&&!reason)return;await fetch(`/api/remediation/${id}/${action}`,{method:"POST",headers:{"Content-Type":"application/json"},body:action==="reject"?JSON.stringify({reason}):undefined});router.refresh();}return <span style={{display:"flex",gap:6}}><button className="btn" onClick={()=>act("approve")}>Approve</button><button className="btn secondary" onClick={()=>act("reject")}>Reject</button></span>}
export function ApplyRemediation({id}:{id:string}){const router=useRouter();return <button className="btn" onClick={async()=>{await fetch(`/api/remediation/${id}/apply`,{method:"POST"});router.refresh();}}>Apply & reassess</button>}
